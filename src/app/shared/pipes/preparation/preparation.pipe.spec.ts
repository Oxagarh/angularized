import { PreparationPipe } from './preparation.pipe';

describe('PreparationPipe', () => {
  it('create an instance', () => {
    const pipe = new PreparationPipe();
    expect(pipe).toBeTruthy();
  });
});
